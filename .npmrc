# Configure GitHub Packages registry for @fieldtest packages
@fieldtest:registry=https://npm.pkg.github.com

# GitHub Packages requires authentication for private packages
# The auth token will be provided via environment variable or CI
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN} 